# DeepSeek TUI 启动脚本说明

## 📁 脚本文件列表

| 脚本文件 | 用途 | 使用场景 |
|---------|------|---------|
| `start-menu.ps1` | **综合启动菜单**（推荐） | 日常使用，提供多种选项 |
| `start-local-llm.ps1` | 启动本地LLM | 使用 http://192.168.2.5:1234 |
| `start-deepseek-cloud.ps1` | 启动DeepSeek官方API | 使用云端最强模型 |
| `test-connection.ps1` | 测试本地LLM连接 | 检查服务是否正常 |

---

## 🚀 快速开始

### 方法1：使用综合菜单（推荐）

```powershell
.\start-menu.ps1
```

然后选择：
- **1** - 使用本地LLM (Qwen3.5-9b)
- **2** - 使用DeepSeek官方API
- **3** - 测试本地LLM连接
- **4** - 检查系统状态

### 方法2：直接启动本地LLM

```powershell
.\start-local-llm.ps1
```

这会直接连接到您的本地服务 `http://192.168.2.5:1234`

### 方法3：测试连接

如果启动失败，先测试连接：

```powershell
.\test-connection.ps1
```

---

## ⚙️ 配置说明

### 本地LLM配置

```powershell
Provider:    openai (兼容模式)
Base URL:    http://192.168.2.5:1234/v1
Model:       qwen3.5-9b-deepseek-v4-flash@q6_k
API Key:     not-needed (本地服务通常不需要)
```

### DeepSeek官方API配置

```powershell
Provider:    deepseek
Base URL:    https://api.deepseek.com/beta
Model:       deepseek-v4-pro (自动模式)
API Key:     需要预先设置
```

---

## 🔧 修改配置

如果需要修改本地LLM地址或模型，编辑对应的 `.ps1` 文件：

```powershell
# 在 start-local-llm.ps1 中修改：
$env:DEEPSEEK_BASE_URL = "http://192.168.2.5:1234/v1"  # 修改地址
$env:DEEPSEEK_MODEL = "qwen3.5-9b-deepseek-v4-flash@q6_k"  # 修改模型
```

---

## 📝 常见问题

### Q1: 提示"无法加载，因为在此系统上禁用了脚本执行"

**解决方法：** 以管理员身份运行PowerShell，然后执行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q2: 连接本地LLM失败

**检查步骤：**
1. 运行 `.\test-connection.ps1` 测试连接
2. 确认本地LLM服务已启动
3. 检查地址 `http://192.168.2.5:1234` 是否正确
4. 检查防火墙设置

### Q3: 如何查看系统状态

运行：
```powershell
.\start-menu.ps1
```
然后选择 **4** - 检查系统状态

或直接运行：
```powershell
deepseek doctor
```

---

## 🎮 启动后的基本操作

进入TUI界面后：

- **直接输入** - 开始对话
- **@文件** - 附加文件上下文
- **/model** - 切换模型
- **/help** - 查看帮助
- **Ctrl+K** - 命令面板
- **Tab** - 自动补全
- **Shift+Tab** - 切换推理深度

---

## 📌 快捷方式创建

### 创建桌面快捷方式

1. 右键桌面 → 新建 → 快捷方式
2. 输入位置：
   ```
   powershell.exe -NoExit -File "F:\ai\codes\github\DeepSeek-TUI-main\DeepSeek-TUI-main\start-menu.ps1"
   ```
3. 命名：`DeepSeek TUI`
4. 完成

### 固定到任务栏

1. 右键PowerShell图标 → 固定到任务栏
2. 右键任务栏图标 → 右键PowerShell → 属性
3. 在"目标"后添加：
   ```
   -NoExit -File "F:\ai\codes\github\DeepSeek-TUI-main\DeepSeek-TUI-main\start-menu.ps1"
   ```

---

## 💡 提示

- 本地LLM完全免费，无需网络连接（离线可用）
- DeepSeek官方API需要API Key，按token计费
- 首次使用建议先用本地LLM测试功能
- 生产环境推荐使用DeepSeek官方API（最强性能）

---

## 🆘 获取帮助

如果遇到问题：

1. 运行 `deepseek doctor` 检查系统状态
2. 查看项目文档：`docs/` 目录
3. 查看README：`README.md`
4. GitHub Issues：https://github.com/Hmbown/DeepSeek-TUI/issues
