# 测试任务 - 验证 autocode 修复

## 任务目标

创建 2 个简单的测试文件，验证 autocode 的路径解析和文件写入是否正常。

## 任务详情

### 任务 1: 创建测试页面
- **文件路径**: `src/pages/test/test.vue`
- **功能**: 简单的测试页面，显示"Autocode Test 1"
- **要求**: 
  - 使用 Vue 3 Composition API
  - 包含 template 和 script
  - 样式简单即可

### 任务 2: 创建测试组件
- **文件路径**: `src/components/TestBox.vue`
- **功能**: 简单的测试组件，接收 title 属性并显示
- **要求**:
  - 使用 Vue 3 Composition API
  - 定义 props: title (String)
  - 显示 title 内容

## 验证要点

1. 文件应该创建在 `f:\ai\codes\github\AILeaningApp` 目录下
2. 不应该创建在 `f:\src\...` 或 `f:\pages\...`
3. 文件内容应该完整且正确
