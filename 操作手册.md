# DeepSeek TUI 完整操作手册

> **版本**: 0.8.37  
> **更新日期**: 2026-05-15  
> **适用环境**: Windows 10/11

---

## 📋 目录

- [一、系统概述](#一系统概述)
- [二、环境准备](#二环境准备)
- [三、快速启动](#三快速启动)
- [四、配置说明](#四配置说明)
- [五、日常使用](#五日常使用)
- [六、常用命令](#六常用命令)
- [七、故障排除](#七故障排除)
- [八、高级功能](#八高级功能)
- [九、最佳实践](#九最佳实践)

---

## 一、系统概述

### 1.1 什么是DeepSeek TUI？

DeepSeek TUI是一个运行在终端的AI编程助手，可以：
- 💬 与AI模型对话，获得代码建议
- 📝 读取和编辑文件
- 🔧 执行Shell命令
- 🔍 搜索网络和文件
- 🤖 自动完成编程任务

### 1.2 系统架构

```
用户输入 → DeepSeek调度器 → TUI界面 → AI模型处理 → 返回结果
```

### 1.3 支持的AI模型

| 模型类型 | 提供商 | 特点 | 成本 |
|---------|--------|------|------|
| **本地LLM** | 自建服务 | 免费、离线可用 | 免费 |
| **DeepSeek V4 Pro** | DeepSeek官方 | 最强推理能力 | 按token计费 |
| **DeepSeek V4 Flash** | DeepSeek官方 | 快速、低成本 | 按token计费 |

---

## 二、环境准备

> 📚 **完整的安装说明请查看**: [docs/zh/安装指南.md](docs/zh/安装指南.md)

### 2.1 系统要求

- **操作系统**: Windows 10/11
- **内存**: 至少4GB RAM
- **磁盘**: 至少2GB可用空间
- **网络**: 访问本地LLM服务或互联网

### 2.2 已安装组件

✅ **Rust**: 1.94.1 (满足1.88+要求)  
✅ **deepseek**: 0.8.37 (CLI调度器)  
✅ **deepseek-tui**: 0.8.37 (TUI运行时)  
✅ **Python**: 已安装 (代码执行工具)  
✅ **Node.js**: 已安装 (代码执行工具)

### 2.3 安装位置

```
程序安装: C:\Users\Administrator\.cargo\bin\
配置文件: C:\Users\Administrator\.deepseek\config.toml
项目目录: F:\ai\codes\github\deepseektuizh\
启动脚本: F:\ai\codes\github\deepseektuizh\*.ps1
```

---

### 2.4 项目安装说明

**重要**: 如果您刚下载或克隆了这个项目，需要先安装！

#### 方式一：使用Cargo安装（推荐）

```powershell
# 切换到项目目录
cd "F:\ai\codes\github\deepseektuizh"

# 编译安装CLI调度器
cargo install --path crates/cli --locked --force

# 编译安装TUI运行时
cargo install --path crates/tui --locked --force

# 验证安装
deepseek --version
deepseek-tui --version
```

#### 方式二：直接编译运行（开发模式）

```powershell
# 切换到项目目录
cd "F:\ai\codes\github\deepseektuizh"

# 编译Release版本
cargo build --release

# 运行程序
.\target\release\deepseek
```

#### 方式三：使用npm安装（如果可用）

```powershell
cd "F:\ai\codes\github\deepseektuizh\npm\deepseek-tui"
npm install
npm link
```

**安装完成后**，您就可以使用下面的启动脚本运行程序了。

---

## 三、快速启动

### 3.1 方式一：使用启动菜单（推荐）

**步骤1：打开PowerShell**

按 `Win + X`，选择 "Windows PowerShell" 或 "终端"

**步骤2：切换到项目目录**

```powershell
cd "F:\ai\codes\github\deepseektuizh"
```
 
**步骤3：运行启动菜单**

```powershell
.\start-menu.ps1
```

**步骤4：选择模式**

```
请选择启动模式:

  [1] 本地LLM (Qwen3.5-9b)          ← 输入 1 回车
  [2] DeepSeek官方API
  [3] 测试本地LLM连接
  [4] 检查系统状态
  [0] 退出
```

**步骤5：等待启动**

程序会自动连接 `http://192.168.2.5:1234` 的本地LLM服务

---

### 3.2 方式二：直接启动本地LLM

```powershell
cd "F:\ai\codes\github\deepseektuizh"
.\start-local-llm.ps1
```

---

### 3.3 方式三：使用DeepSeek官方API

**前提**: 需要有DeepSeek API Key

```powershell
cd "F:\ai\codes\github\deepseektuizh"
.\start-deepseek-cloud.ps1
```

首次运行会提示输入API Key，获取地址：https://platform.deepseek.com/api_keys

---

### 3.4 首次启动引导

程序启动后会显示5步引导：

```
Step 1/5: 欢迎界面
  → 按 Enter 继续

Step 2/5: API Key设置
  → 本地LLM: 输入任意字符（如 "not-needed"）
  → 官方API: 输入真实API Key

Step 3/5: 工作目录信任
  → 按 Enter 信任当前目录

Step 4/5: 功能配置
  → 按 Enter 使用默认配置

Step 5/5: 完成
  → 进入主界面
```

---

## 四、配置说明

### 4.1 本地LLM配置

**当前配置**:

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **服务地址** | `http://192.168.2.5:1234/v1` | 本地LLM服务API端点 |
| **模型名称** | `qwen3.5-9b-deepseek-v4-flash@q6_k` | 使用的模型 |
| **Provider** | `openai` | 使用OpenAI兼容模式 |
| **API Key** | `not-needed` | 本地服务不需要 |

**修改配置**:

编辑 `start-local-llm.ps1` 文件：

```powershell
# 第6行 - 修改服务地址
$env:DEEPSEEK_BASE_URL = "http://新IP:新端口/v1"

# 第7行 - 修改模型名称
$env:DEEPSEEK_MODEL = "新模型名称"
```

---

### 4.2 DeepSeek官方API配置

**配置项**:

| 配置项 | 值 | 说明 |
|--------|-----|------|
| **服务地址** | `https://api.deepseek.com/beta` | DeepSeek官方API |
| **默认模型** | `deepseek-v4-pro` | 旗舰推理模型 |
| **API Key** | 需要申请 | 从官网获取 |

**获取API Key**:

1. 访问 https://platform.deepseek.com
2. 注册/登录账号
3. 进入 "API Keys" 页面
4. 点击 "Create API Key"
5. 复制并保存Key（只显示一次）

---

### 4.3 配置文件位置

```
C:\Users\Administrator\.deepseek\config.toml
```

**查看配置**:

```powershell
Get-Content "$env:USERPROFILE\.deepseek\config.toml"
```

---

## 五、日常使用

### 5.1 主界面说明

启动成功后进入TUI主界面，采用**双栏布局**：

```
┌──────────────────────────────────────┬──────────────────────────────┐
│         顶部状态栏 (Header)           │        Tasks (任务面板)      │
│  [Mode] [Model] [Cost] [Cache]      │  - 当前任务状态              │
├──────────────────────────────────────┤  - 后台作业列表              │
│                                       │  - 错误日志                  │
│         主对话区 (Main)               │  - 活动记录                  │
│         - 对话历史                     │  - 子代理状态                │
│         - AI 回复                     │                              │
│         - 工具调用                     │                              │
│         - 思考过程                     │                              │
│                                       │                              │
├──────────────────────────────────────┤                              │
│         输入区 (Composer)             │                              │
│  > 在这里输入您的问题...              │                              │
└──────────────────────────────────────┴──────────────────────────────┘
         左侧 (主区域)                           右侧 (Work/Tasks)
```

**界面区域说明**：

| 区域 | 位置 | 功能 |
|------|------|------|
| **顶部状态栏** | 顶部 | 显示模式、模型、缓存状态、工作时间 |
| **主对话区** | 左侧 | 显示对话历史、AI回复、工具调用、思考过程 |
| **任务面板** | 右侧 | 显示任务状态、后台作业、错误日志、活动记录 |
| **输入区** | 底部 | 输入消息、命令、附加文件 |

> 📚 **详细布局说明**: [docs/zh/界面布局说明.md](docs/zh/界面布局说明.md)

### 5.2 基本对话

**简单对话**:

```
> 你好，请介绍一下自己
```

**附加文件**:

```
> @README.md 请总结这个文件的内容
```

**多行输入**:

```
> 请帮我写一个Python函数
  要求：
  1. 计算斐波那契数列
  2. 返回前n项
  3. 包含注释
  
  [按 Enter 发送]
```

### 5.3 文件操作

**读取文件**:

```
> @src/main.py 这个文件是做什么的？
```

**编辑文件**:

```
> 请在 README.md 中添加安装说明
```

AI会请求您的批准后执行编辑。

### 5.4 代码执行

**运行Python代码**:

```
> 请运行这段Python代码：print("Hello World")
```

**执行Shell命令**:

```
> 列出当前目录的所有.py文件
```

AI会使用 `exec_shell` 工具执行命令，需要您批准。

---

## 六、常用命令

### 6.1 斜杠命令

在输入框中以 `/` 开头的命令：

| 命令 | 功能 | 示例 |
|------|------|------|
| `/help` | 显示帮助 | `/help` |
| `/model` | 切换模型 | `/model auto` |
| `/provider` | 切换提供商 | `/provider deepseek` |
| `/config` | 查看/修改配置 | `/config` |
| `/compact` | 压缩上下文 | `/compact` |
| `/theme` | 切换主题 | `/theme dark` |
| `/clear` | 清空对话 | `/clear` |
| `/exit` | 退出程序 | `/exit` |

### 6.2 快捷键

| 快捷键 | 功能 |
|--------|------|
| `Enter` | 发送消息 |
| `Shift + Enter` | 换行 |
| `Tab` | 自动补全 |
| `Shift + Tab` | 切换推理深度 (off → high → max) |
| `Ctrl + K` | 命令面板 |
| `Ctrl + C` | 退出程序 |
| `Esc` | 取消/返回 |
| `↑` | 历史消息 |
| `@` | 附加文件 |

### 6.3 工作模式

| 模式 | 说明 | 适用场景 |
|------|------|---------|
| **Plan** 🔍 | 只读调查，不修改文件 | 代码审查、问题诊断 |
| **Agent** 🤖 | 交互式，需要批准 | 日常编程（默认） |
| **YOLO** ⚡ | 自动批准所有操作 | 受信任的工作环境 |

**切换模式**:

- 在输入框按 `Tab` 键循环切换
- 或使用命令：`/mode plan`、`/mode agent`、`/mode yolo`

---

## 七、故障排除

### 7.1 连接本地LLM失败

**症状**: 启动后提示连接错误或超时

**解决步骤**:

```powershell
# 1. 测试连接
.\test-connection.ps1

# 2. 检查本地服务是否运行
# 在浏览器访问: http://192.168.2.5:1234

# 3. 检查网络连通性
ping 192.168.2.5

# 4. 检查防火墙设置
# Windows Defender → 允许应用通过防火墙
```

**常见原因**:
- 本地LLM服务未启动
- IP地址或端口错误
- 两台设备不在同一网络
- 防火墙阻止访问

---

### 7.2 API Key错误

**症状**: 提示 "Invalid API Key" 或 "Unauthorized"

**解决方法**:

```powershell
# 重新设置API Key
deepseek auth set --provider deepseek

# 或清除旧配置
Remove-Item "$env:USERPROFILE\.deepseek\config.toml"

# 重新启动程序
.\start-menu.ps1
```

---

### 7.3 程序崩溃或卡死

**症状**: 程序无响应或突然退出

**解决方法**:

```powershell
# 1. 强制关闭
Ctrl + C

# 2. 检查系统状态
deepseek doctor

# 3. 查看日志
Get-Content "$env:USERPROFILE\.deepseek\logs\*.log" -Tail 50

# 4. 重启程序
.\start-menu.ps1
```

---

### 7.4 PowerShell脚本无法执行

**症状**: "无法加载，因为在此系统上禁用了脚本执行"

**解决方法**:

```powershell
# 以管理员身份运行PowerShell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

# 确认更改
Y
```

---

### 7.5 模型响应慢

**原因和解决**:

| 原因 | 解决方法 |
|------|---------|
| 本地LLM性能有限 | 切换到DeepSeek官方API |
| 网络延迟 | 检查网络连接 |
| 上下文过长 | 使用 `/compact` 压缩 |
| 推理深度设置为max | 按 `Shift+Tab` 切换到 high 或 off |

---

## 八、高级功能

### 8.1 子代理（Sub-agents）

可以启动多个后台任务并行执行：

```
> 请分析这个项目的代码结构，同时检查是否有潜在bug
```

AI会自动使用 `agent_open` 工具创建子任务。

---

### 8.2 MCP服务器

支持Model Context Protocol扩展：

```powershell
# 查看MCP服务器
deepseek mcp list

# 初始化MCP配置
deepseek mcp init
```

配置文件位置：`C:\Users\Administrator\.deepseek\mcp.json`

---

### 8.3 Skills技能系统

安装和使用技能包：

```
/skills              # 查看已安装的技能
/skill new           # 创建新技能
/skill install github:owner/repo  # 安装社区技能
```

---

### 8.4 会话管理

**保存会话**:

程序自动保存会话到：`C:\Users\Administrator\.deepseek\sessions\`

**恢复会话**:

```powershell
# 列出所有会话
deepseek sessions

# 恢复最近会话
deepseek resume --last

# 恢复指定会话
deepseek resume <SESSION_ID>
```

**在TUI中恢复**:

按 `Ctrl + R` 打开会话选择器

---

### 8.5 工作区快照

每次编辑都会自动创建快照，可以回滚：

```
/restore             # 查看可恢复的快照
/restore 3           # 恢复到第3次操作前
```

快照位置：`C:\Users\Administrator\.deepseek\snapshots\`

---

## 九、最佳实践

### 9.1 提高工作效率

1. **使用 @文件** - 直接附加相关文件，避免重复描述
2. **明确需求** - 越具体的问题，答案越准确
3. **分步执行** - 复杂任务拆分为小步骤
4. **使用Auto模式** - 让AI自动选择合适的模型和推理深度

### 9.2 节省成本（使用官方API时）

1. **使用Flash模型** - 日常任务用 `deepseek-v4-flash`
2. **降低推理深度** - 简单任务用 `off` 或 `high`
3. **定期压缩上下文** - 使用 `/compact`
4. **避免重复对话** - 恢复旧会话而不是重新描述

### 9.3 安全建议

1. **审查AI的操作** - 特别是文件修改和命令执行
2. **使用沙盒模式** - 默认 `workspace-write` 限制在工作区
3. **定期备份** - 重要文件使用git或手动备份
4. **保护API Key** - 不要泄露或提交到代码仓库

### 9.4 本地LLM使用建议

1. **适合场景**: 代码补全、简单问答、离线工作
2. **不适合**: 复杂推理、最新知识、高精度要求
3. **模型选择**: 根据显存选择合适大小的模型
4. **定期更新**: 关注模型更新以获得更好的性能

---

## 附录

### A. 脚本文件清单

| 文件 | 用途 |
|------|------|
| `start-menu.ps1` | 综合启动菜单（推荐） |
| `start-local-llm.ps1` | 直接启动本地LLM |
| `start-deepseek-cloud.ps1` | 启动DeepSeek官方API |
| `test-connection.ps1` | 测试本地LLM连接 |

### B. 重要路径

```
程序:        C:\Users\Administrator\.cargo\bin\deepseek.exe
配置:        C:\Users\Administrator\.deepseek\config.toml
会话:        C:\Users\Administrator\.deepseek\sessions\
快照:        C:\Users\Administrator\.deepseek\snapshots\
技能:        C:\Users\Administrator\.deepseek\skills\
项目:        F:\ai\codes\github\deepseektuizh\
脚本:        F:\ai\codes\github\deepseektuizh\*.ps1
```

### C. 常用命令速查

```powershell
# 启动程序
.\start-menu.ps1

# 测试连接
.\test-connection.ps1

# 检查系统
deepseek doctor

# 查看版本
deepseek --version

# 设置API Key
deepseek auth set --provider deepseek

# 查看帮助
deepseek --help
```

### D. 获取帮助

- **项目文档**: `docs/` 目录
- **README**: `README.md`
- **GitHub Issues**: https://github.com/Hmbown/DeepSeek-TUI/issues
- **配置示例**: `config.example.toml`

---

## 版本历史

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-05-15 | 初始版本，基于v0.8.37 |

---

**文档维护**: 如有问题或建议，请联系系统管理员

**最后更新**: 2026年5月15日
